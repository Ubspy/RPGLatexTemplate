\PassOptionsToPackage{table}{xcolor}
\PassOptionsToPackage{toc}{multitoc}

% Helps with tables somehow idk
\RequirePackage{array}

% Package for setting background image
\RequirePackage{background}

% Helps with bookmarks for labels
\RequirePackage{bookmark}

% Required for some xcolor functionality
\RequirePackage{colortbl}

% Make it so we can have stylalized lists
\RequirePackage{enumitem}

% Lets us use addvspace to add vertical spacing where we want it
\RequirePackage{etoolbox}

% Helps to set page geometry like margins and such
\RequirePackage{geometry}

% Set location of images folder
\RequirePackage{graphicx}

% Make it so we can have more variation on headers and footers
\RequirePackage {fancyhdr}

% Load the font encoder package, required to render custom fonts
\RequirePackage[T1]{fontenc}

% Allow hanging paragraphs
\RequirePackage{hang}

% Add links to parts of the pdf
\RequirePackage{hyperref}

% Package for drop caps
\RequirePackage{lettrine}

% Improve ragged2e hyphenation and overfull boxes
\RequirePackage{microtype}

% Allows small sections with multiple columns
\RequirePackage{multicol}

% Puts table of contents into multiple columns
\RequirePackage{multitoc}

% Allows for left right and center justification
\RequirePackage{ragged2e}

% Specific package for the type of drop cap we want
\RequirePackage{Royal}

% For drawing lines and other special shapes
\RequirePackage{tikz}
\usetikzlibrary{positioning}
\usetikzlibrary{calc}

% Create styled textboxes
\RequirePackage [breakable,skins,xparse]{tcolorbox}

% For making tables with specific spacing
\RequirePackage{tabularx}

% To get section and chapter names
\RequirePackage{titlesec}

% Package for defining all the colors
\RequirePackage{xcolor}

% Import base packages first
\RequirePackage{lib/colors}
\RequirePackage{lib/constants}
\RequirePackage{lib/fonts}
\RequirePackage{lib/page-style}

% Then everything else
\RequirePackage{lib/boxes}
\RequirePackage{lib/footers}
\RequirePackage{lib/sections}
\RequirePackage{lib/statblock}
\RequirePackage{lib/tables}